generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id                  String   @id @default(cuid())
  password            String
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  username            String   @unique
  canDeleteBanners    Boolean  @default(true)
  canDeleteCategories Boolean  @default(true)
  canDeleteOrders     Boolean  @default(true)
  canDeleteProducts   Boolean  @default(true)
  canDeletePromoCodes Boolean  @default(true)
  canManageBanners    Boolean  @default(true)
  canManageCategories Boolean  @default(true)
  canManageOrders     Boolean  @default(true)
  canManageProducts   Boolean  @default(true)
  canManagePromoCodes Boolean  @default(true)
  role                Role     @default(ADMIN)
  canDeleteBrands     Boolean  @default(true)
  canManageBrands     Boolean  @default(true)
  canManageReviews    Boolean  @default(true)
}

model MainCategory {
  id          String     @id @default(cuid())
  name        String     @unique
  slug        String     @unique
  description String?
  image       String?
  isActive    Boolean    @default(true)
  showInNav   Boolean    @default(true)
  navOrder    Int        @default(0)
  isFeatured  Boolean    @default(false)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  categories  Category[]
  products    Product[]
  brands      Brand[]
}

model Category {
  id          String    @id @default(cuid())
  name        String
  description String?
  image       String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  isFeatured  Boolean   @default(false)
  slug        String    @unique
  brandId     String
  brand       Brand     @relation(fields: [brandId], references: [id])
  products    Product[]
  mainCategoryId String?
  mainCategory   MainCategory? @relation(fields: [mainCategoryId], references: [id])

  @@unique([brandId, name])
  @@index([brandId])
}

model Product {
  id            String      @id @default(cuid())
  name          String
  slug          String      @unique
  images        String
  isTrending    Boolean     @default(false)
  description   String?
  price         Decimal     @db.Decimal(10, 2)
  discountPrice Decimal?    @db.Decimal(10, 2)
  discountType  String?
  discountValue Decimal?    @db.Decimal(10, 2)
  stock         Int         @default(0)
  categoryId    String
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  sku           String?
  brandId       String
  orderItems    OrderItem[]
  brand         Brand       @relation(fields: [brandId], references: [id])
  category      Category    @relation(fields: [categoryId], references: [id])
  mainCategoryId String?
  mainCategory   MainCategory? @relation(fields: [mainCategoryId], references: [id])
  reviews       Review[]

  @@index([brandId])
  @@index([categoryId])
  @@index([name])
}

model Brand {
  id          String     @id @default(cuid())
  name        String     @unique
  slug        String     @unique
  description String?
  image       String?
  group       BrandGroup @default(DIFFERENT)
  isActive    Boolean    @default(true)
  isFeatured  Boolean    @default(false)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  categories  Category[]
  products    Product[]
  mainCategoryId String?
  mainCategory   MainCategory? @relation(fields: [mainCategoryId], references: [id])

  @@index([group])
  @@index([isActive])
  @@index([isFeatured])
}

model Order {
  id            String      @id @default(cuid())
  totalAmount   Decimal     @db.Decimal(10, 2)
  status        OrderStatus @default(PENDING)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  city          String
  streetAddress String
  Name          String
  phone         String
  discount      Decimal     @default(0) @db.Decimal(10, 2)
  promoCodeId   String?
  promoCode     PromoCode?  @relation(fields: [promoCodeId], references: [id])
  items         OrderItem[]
}

model Banner {
  id         String   @id @default(cuid())
  title      String?
  subtitle   String?
  titleAr    String?
  subtitleAr String?
  image      String
  buttonText String?  @default("Shop Now")
  link       String?  @default("/products")
  badge      String?  @default("New Collection")
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model PromoCode {
  id                 String   @id @default(cuid())
  code               String   @unique
  discountPercentage Int
  delegateName       String?
  isActive           Boolean  @default(true)
  usageCount         Int      @default(0)
  totalSales         Decimal  @default(0) @db.Decimal(10, 2)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  orders             Order[]
}

model OrderItem {
  id        String   @id @default(cuid())
  orderId   String
  productId String
  quantity  Int
  price     Decimal  @db.Decimal(10, 2)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product   Product  @relation(fields: [productId], references: [id])

  @@index([orderId])
  @@index([productId])
}

model Settings {
  id                        String   @id @default("site-settings")
  categoriesCtaTitle        String?  @default("Need expert advice?")
  categoriesCtaDesc         String?  @default("Our beauty consultants are here to help you find the perfect products for your skin type and concerns.")
  categoriesCtaTitleAr      String?  @default("هل تحتاجين إلى نصيحة الخبراء؟")
  categoriesCtaDescAr       String?  @default("مستشارو التجميل لدينا هنا لمساعدتك في العثور على المنتجات المثالية لنوع بشرتك واحتياجاتها.")
  updatedAt                 DateTime @updatedAt
  categoriesCtaImage        String?  @default("https://lh3.googleusercontent.com/aida-public/AB6AXuC-S_GMsoebb73JIEWcxtvH2G-vVgkfypE8ysWpGMNiiiwyTno8rIbMCpHR-fsa76ZQL49aYswb7bGZh-kgwc6z9lv0VwUSUrStxNWz2qU3RuIb75ShOMAKZMRyrOXZHZjEBgtxfW7r97FEEshOkEd2MqgE6FpGYrmKa8msLtMOQxXBsmhr3ZGGEtL7jpzgMYbgrAXhiHcMfCspdvD5FRNuSbgFY9_xGqcJM9KbgG0MoC4Ie4WkkmCR4FsuavfglcnY13G2ADZxlK8F")
  expressShippingTime       String?  @default("1-2 Business Days")
  finalSaleDesc             String?  @default("Certain items like opened skincare and personalized products are final sale for hygiene reasons.")
  finalSaleDescAr           String?  @default("بعض العناصر مثل منتجات العناية بالبشرة المفتوحة والمنتجات المخصصة هي بيع نهائي لأسباب صحية.")
  finalSaleTitle            String?  @default("Final Sale Items")
  finalSaleTitleAr          String?  @default("أصناف البيع النهائي")
  returnsDesc               String?  @default("Your satisfaction is our priority. If you're not happy with your purchase, we're here to help.")
  returnsDescAr             String?  @default("رضاكم هو أولويتنا. إذا لم تكن راضيًا عن مشترياتك، فنحن هنا للمساعدة.")
  returnsTitle              String?  @default("Returns & Exchanges")
  returnsTitleAr            String?  @default("المرتجعات والاستبدال")
  shippingDesc              String?  @default("We ensure your beauty essentials reach you in perfect condition, wherever you are in the world.")
  shippingDescAr            String?  @default("نحن نضمن وصول مستلزمات التجميل الخاصة بك إليك في حالة ممتازة، أينما كنت في العالم.")
  shippingTitle             String?  @default("Fast & Reliable Shipping")
  shippingTitleAr           String?  @default("شحن سريع وموثوق")
  standardShippingTime      String?  @default("3-5 Business Days")
  verificationDesc          String?  @default("Orders are processed within 24-48 hours. You will receive a confirmation email once your package has shipped.")
  verificationDescAr        String?  @default("يتم معالجة الطلبات في غضون 24-48 ساعة. ستتلقى رسالة تأكيد بالبريد الإلكتروني بمجرد شحن طردك.")
  verificationTitle         String?  @default("Verification Process")
  verificationTitleAr       String?  @default("عملية التحقق")
  hygieneDesc               String?  @default("For your safety and to maintain the highest hygiene standards, we follow strict protocols for handling beauty products.")
  hygieneDescAr             String?  @default("من أجل سلامتك وللحفاظ على أعلى معايير النظافة، نتبع بروتوكولات صارمة للتعامل مع منتجات التجميل.")
  hygieneTitle              String?  @default("Hygiene & Safety Protocols")
  hygieneTitleAr            String?  @default("بروتوكولات النظافة والسلامة")
  shippingReturnsImage      String?  @default("https://lh3.googleusercontent.com/aida-public/AB6AXuC1GmfD6bueEsJqlHNPjDWHMlhsLZSm2Jmp21TUCLKvobkcd7oAPMMdwzfm8BOHC5XtR0EP6tLI7DT5hhyLxuijsbpX2kQf6iNlqROU-8k-DrqZAUqdc7-0lE4nxuCcLaEb0fEaXVBxc_yXkiUlyhfvaYJ1FfHZtngnoJbeanLgsf7rcxqON6rjkoC4BQv6FhlwLNKZrMbxjCugphq-bo5GCqBoLfmjjZSuH0N5eV-Kz33xFQTD5jSYCTsVYAwOkwhLQsQiPD_lnD9U")
  aboutHeroImage            String?  @default("https://lh3.googleusercontent.com/aida-public/AB6AXuAz8qN2iAHz-UZeEQfqOY49U5OCZ5z4ejVm7ILFjFSl9S5xg_6UuBa61qOmrkMPrBa4CuXDzHa9EN3-LNyUxi5IDK5A9TvJWkNuG-tt_RRyvJH8LvynO1daOEkTk47KDtkW3Md2ugZYShZJdxolsjiJUtDdOOz4Q7-6TNrexIvyClP0ADf1TWdbCUk1kBn8bfzhTC1cn8s9jG3yt0tDDht7__J5YKKf690SmKN4WIJX_pc2LOj3x1CnYk5JuqEu0Bzp2vGwsrYLaJWb")
  aboutHeroSubtitle         String?  @default("Redefining beauty with clean, conscious care that honors your skin and the earth.")
  aboutHeroSubtitleAr       String?  @default("إعادة تعريف الجمال بعناية نظيفة وواعية تكرم بشرتك والأرض.")
  aboutHeroTitle            String?  @default("Our Story")
  aboutHeroTitleAr          String?  @default("قصتنا")
  aboutNarrativeDesc1       String?  @default("We believe that beauty should be kind—to your skin, your spirit, and the planet. Our journey started in a sun-drenched studio with a simple, radical goal: to create high-performance skincare without compromising on ethics or transparency.")
  aboutNarrativeDesc1Ar     String?  @default("نؤمن بأن الجمال يجب أن يكون لطيفًا - على بشرتك، وروحك، والكوكب. بدأت رحلتنا في استوديو مشمس بهدف بسيط وجذري: إنشاء عناية بالبشرة عالية الأداء دون المساومة على الأخلاق أو الشفافية.")
  aboutNarrativeDesc2       String?  @default("For too long, the industry was clouded by hidden ingredients and unsustainable practices. Ruby Beauty was born to bring light back to your ritual. Every formula is meticulously crafted with botanical extracts and clean science, ensuring that what you put on your body is as pure as the results it delivers.")
  aboutNarrativeDesc2Ar     String?  @default("لفترة طويلة، كانت الصناعة ملبدة بالمكونات الخفية والممارسات غير المستدامة. ولدت روبي بيوتي لإعادة النور إلى طقوسك. يتم صياغة كل تركيبة بدقة باستخدام المستخلصات النباتية والعلوم النظيفة، مما يضمن أن ما تضعه على جسمك نقي بقدر النتائج التي يقدمها.")
  aboutNarrativeImage       String?  @default("https://lh3.googleusercontent.com/aida-public/AB6AXuC4yp4c_LJLNPwaV2ay8DZ6xRHD0UF1WqXU8eDtrdDoiVjtq9oNRc9Cn6cnbqsNwOLO-y-99jnkiLnCsGLs2rQqthU8TPqhAh2Msisbst1UyfyrILBR5fRO7KYu90u1FEoeRRjGceGVbB5vz2SJAtjzUrLLtA6BmR8VN5a5Seo4MraBJj7i4Gs4QPEZbURtSN-F7wbJsu4WNj3pEaWlye2SuJvokQhYXJ27gnAoabHg5_0_4DZY49qyKnQuMHHL9atOIILRIMD3FkeZ")
  aboutNarrativeQuote       String?  @default("Naturally inspired, scientifically proven.")
  aboutNarrativeQuoteAr     String?  @default("مستوحى طبيعياً، مثبت علمياً.")
  aboutNarrativeTitle       String?  @default("Our Mission for Clean Beauty")
  aboutNarrativeTitleAr     String?  @default("مهمتنا للجمال النظيف")
  aboutValue1Desc           String?  @default("We never test on animals. Our products are certified cruelty-free by Leaping Bunny.")
  aboutValue1DescAr         String?  @default("نحن لا نختبر أبدًا على الحيوانات. منتجاتنا معتمدة خالية من القسوة من قبل Leaping Bunny.")
  aboutValue1Title          String?  @default("Cruelty-Free")
  aboutValue1TitleAr        String?  @default("خالٍ من القسوة")
  aboutValue2Desc           String?  @default("No animal-derived ingredients. Just pure, potent plant power.")
  aboutValue2DescAr         String?  @default("لا توجد مكونات مشتقة من الحيوانات. فقط قوة نباتية نقية وفعالة.")
  aboutValue2Title          String?  @default("100% Vegan")
  aboutValue2TitleAr        String?  @default("نباتي 100٪")
  aboutValue3Desc           String?  @default("Eco-friendly packaging and responsibly sourced ingredients.")
  aboutValue3DescAr         String?  @default("تغليف صديق للبيئة ومكونات من مصادر مسؤولة.")
  aboutValue3Title          String?  @default("Sustainable")
  aboutValue3TitleAr        String?  @default("مستدام")
  aboutValuesDesc           String?  @default("We are committed to transparency, sustainability, and ethical practices in everything we do.")
  aboutValuesDescAr         String?  @default("نحن ملتزمون بالشفافية والاستدامة والممارسات الأخلاقية في كل ما نقوم به.")
  aboutValuesTitle          String?  @default("Our Core Values")
  aboutValuesTitleAr        String?  @default("قيمنا الجوهرية")
  aboutNarrativeFounded     String?  @default("Founded in 2024")
  aboutNarrativeFoundedAr   String?  @default("تأسست في 2024")
  footerBrandTitle          String?  @default("Ruby Beauty")
  footerBrandTitleAr        String?  @default("Ruby Beauty")
  footerBrandDescription    String?  @default("Premium botanical skincare designed to reveal your natural radiance. Cruelty-free, vegan, and sustainable.")
  footerBrandDescriptionAr  String?  @default("منتجات عناية بالبشرة نباتية فاخرة مصممة لإظهار إشراقتك الطبيعية. خالية من القسوة، نباتية ومستدامة.")
  footerCopyright           String?  @default("© 2026 Ruby Beauty. All rights reserved.")
  footerCopyrightAr         String?  @default("© 2026 روبي بيوتي. جميع الحقوق محفوظة.")
  footerInstagramUrl        String?  @default("https://www.instagram.com/ruby.beauty.sy")
  footerFacebookUrl         String?  @default("https://www.facebook.com/share/1HzXdo7sLG/?mibextid=wwXIfr")
  footerWhatsappUrl         String?  @default("https://wa.me/963933254796")
  footerShopTitle           String?  @default("Shop")
  footerShopTitleAr         String?  @default("المتجر")
  footerSupportTitle        String?  @default("Support")
  footerSupportTitleAr      String?  @default("الدعم")
  footerCompanyTitle        String?  @default("Company")
  footerCompanyTitleAr      String?  @default("الشركة")
  footerSupportLink1Label   String?  @default("Help Center")
  footerSupportLink1LabelAr String?  @default("مركز المساعدة")
  footerSupportLink1Url     String?  @default("https://wa.me/963933254796")
  footerSupportLink2Label   String?  @default("Shipping & Returns")
  footerSupportLink2LabelAr String?  @default("الشحن والإرجاع")
  footerSupportLink2Url     String?  @default("/shipping-returns")
  footerSupportLink3Label   String?  @default("Contact Us")
  footerSupportLink3LabelAr String?  @default("اتصل بنا")
  footerSupportLink3Url     String?  @default("https://wa.me/963933254796")
  footerCompanyLink1Label   String?  @default("About Us")
  footerCompanyLink1LabelAr String?  @default("من نحن")
  footerCompanyLink1Url     String?  @default("/about-us")
  footerCompanyLink2Label   String?
  footerCompanyLink2LabelAr String?
  footerCompanyLink2Url     String?
  footerCompanyLink3Label   String?
  footerCompanyLink3LabelAr String?
  footerCompanyLink3Url     String?
  footerCategory1Id         String?
  footerCategory2Id         String?
  footerCategory3Id         String?
  footerCategory4Id         String?
  exchangeRate              Decimal  @default(135) @db.Decimal(10, 2)
  middleBanner1Image        String?  @default("https://images.unsplash.com/photo-1596462502278-27bfdc403348?w=1200")
  middleBanner1Link         String?  @default("/products")
  middleBanner2ButtonText   String?  @default("Shop Perfumes")
  middleBanner2ButtonTextAr String?  @default("تسوق العطور")
  middleBanner2Image        String?  @default("https://images.unsplash.com/photo-1547887538-e3a2f32cb1cc?w=1200")
  middleBanner2Link         String?  @default("/products")
  middleBanner2Subtitle     String?  @default("Discover scents that last all day and leave a mark.")
  middleBanner2SubtitleAr   String?  @default("اكتشف الروائح التي تدوم طوال اليوم وتترك أثراً.")
  middleBanner2Title        String?  @default("Luxury Fragrances")
  middleBanner2TitleAr      String?  @default("عطور فاخرة")
}

enum OrderStatus {
  PENDING
  PROCESSING
  SHIPPED
  DELIVERED
  CANCELLED
}

enum Role {
  SUPER_ADMIN
  ADMIN
}

enum BrandGroup {
  MAIN
  DIFFERENT
}

model Review {
  id          String   @id @default(cuid())
  productId   String
  rating      Int
  feedback    String?
  image       String?
  name        String
  email       String?
  isApproved  Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@index([productId])
}
